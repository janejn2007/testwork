public class UserApiClient {
    private static UserApiClient instance;
    private final String baseUrl;
    private int timeout = 5000;

    private UserApiClient(String baseUrl) {
        this.baseUrl = baseUrl;
    }

    public static UserApiClient getInstance(String baseUrl) {
        if (instance == null) {
            instance = new UserApiClient(baseUrl);
        }
        return instance;
    }

    public Response createUser(Map<String, Object> payload) {
        return given()
            .baseUri(baseUrl)
            .contentType(ContentType.JSON)
            .body(payload)
            .when()
            .post("/api/v1/users");
    }

    public Response getUserById(String userId) {
        return given()
            .baseUri(baseUrl)
            .pathParam("id", userId)
            .when()
            .get("/api/v1/users/{id}");
    }

    public void deleteUser(String userId) {
        given()
            .baseUri(baseUrl)
            .when()
            .delete("/api/v1/users/" + userId);
    }
}

1. Используются приватные переменные, но нет к ним геттеров и  сеттеров, учитывая, что используется паттерн синглтон,  сомнительное  решение. 
2. Введена переменная timeout, которая захардкодена и нигде не испольуется. 
3. Не очень понятное название параметра, который используется при создании пользователя. 
Судя по названию пользователь создается  по  платежу?  Наверное, это не лучший вариант названия  переменной, как минимум. 
А если все  таки все верно, то  тогда возникают  вопросики,  как  выглядит этот объект,  и нет ли в нем  избыточности данных

4. В коде повторяется строка в нескольких  методах "/api/v1/users". Хорошо бы  ее  вынести либо в отдельную константу,  либо в  basePath
5. Метод delete не  возвращает статус  код, будет непоняно насколько успешно  прошло  удаление. 
6. В методах нет проверок, на  содержимое строк, которые приходят в  параметрах. Вдруг придет пустая  строка или " ", или null.
7. К методам можно  добавить логирование. Хотя бы  в  случае  ошибок. 
8. Возвращаясь к использованию синглтонов, думаю не очень уместно его использовать в этом  случае. 
Во первых, это помешает вести  тестирование. Создается один обект, в котором жестко прописан будет baseUrl. Доступа к нему из вне нет. По другому url  при необходимости уже обратиться не  получится.
Во вторых,  синглтон в принципе является иногда  антипаттерном, и это как раз тот  случай. Нарушаются принципы  SOLID.  Класс и реализует вызов методов Rest, и следит за количеством  своих  экемпляров

Вопросы на обсуждение с командой: 
1. Обсудить использование синглтонов в архитектуре. 
2. Обсудить на основании какого объекта создается пользователь 
3. В методы с удалением сущностей добавить статус  коды.  

Предложения по рефакторигу: 
1. Добавит логирование
2. Добавить проверки параметров  методов
3. Вынести basePath
4. Добавить использование статус  кодов
5. Подумать над синглтоном


@ExtendWith(SeleniumExtension.class)
public class BaseTest {

    protected WebDriver driver;
    protected UserApiClient apiClient;

    @BeforeEach
    void setUp() {
        System.setProperty("webdriver.chrome.driver", "/usr/local/bin/chromedriver");
        driver = new ChromeDriver();
        driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
        apiClient = UserApiClient.getInstance("http://localhost:8080");
    }

    @AfterEach
    void tearDown() {
        driver.quit();
    }
}

1. Судя по коду, это явно BaseTest для UI  тестов,  можно указать это в  названии класса.  
BaseTest выносится обычно то, что  касается всех тестов. Тесты могут быть на бэк,  без вызова браузера

2. @ExtendWith(SeleniumExtension.class)
уместно ли испозование этого класса ?  Есть  какие то  особые  настройки, туда  вынесенные 

3. System.setProperty("webdriver.chrome.driver", "/usr/local/bin/chromedriver"); - почему так ? Почему не импользовать WebDriverManager

4. Прописано жестко местонахождение драйвера, предполагаю, что такое может сработать на  машине локально, но есть риски, что упадет при запуске в другом  месте.

5. Возникают  вопросы, почему в методе @BeforeEach происходит каждый раз перед каждым тестом установка  свойств дравйвера и его вызов. 
По поводу вызова тут можно  подумать, так как  если тестов мало, то  это  гарантирует изолированность
Но есть ли количество тестов со временем вырастет, то  время  прогона будет сильно  больще, чем  если будет чиститься состояние  браузера после каждого теста. 
Глядя на driver.quit(); после каждого теста посещают такие  же  сомнения. Насколько это  уместно 


6.  Базовый класс для UI инициализирует apiClient, причем для  конкретного контроллера. Это является нарушением приницпов SOLID (единственной отвественности )  

Предложеня по рефакторингу: 
1. Использовать WebDriverManager
2. Не использовать прописывание путей, но  использование WebDriverManager как раз таки поможет для этого 
3. Разделить базовые  классы для  UI  тестов и тестов в  целом.
4. Продумать как использовать браузер, стоит ли  его инициализировать с нуля при  каждом  тесте. 